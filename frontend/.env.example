# API URL — leave empty for same-origin (development with Vite proxy)
# Set to full URL for production or when backend runs on different port
# Example: VITE_API_URL=https://api.sigscore.dev
VITE_API_URL=

# Sentry error monitoring
VITE_SENTRY_DSN=
