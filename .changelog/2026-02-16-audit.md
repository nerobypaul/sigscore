## 19:30 — Production Deployment Audit for Show HN Launch

**Task:** Audit the DevSignal production deployment at https://energetic-wisdom-production.up.railway.app for Show HN launch readiness

**Findings:**

### ✅ PASSING CHECKS

1. **Landing Page HTML (200 OK)**
   - All meta tags properly configured for social sharing
   - Title: "DevSignal — Developer Signal Intelligence"
   - Description accurately reflects the product
   - OG image tags present: https://devsignal.dev/og-image.png (1200x630)
   - Twitter card tags properly configured
   - No localhost references in HTML
   - Inline SVG favicon working
   - Theme color set to #4f46e5 (brand indigo)

2. **API Documentation (200 OK)**
   - GET /api-docs/ returns proper Swagger UI HTML
   - GET /api-docs.json returns valid OpenAPI 3.0.3 spec
   - Server URL configured as "/api/v1" (relative, works correctly)
   - Complete schema definitions for all endpoints
   - 9 API tags defined (Auth, Contacts, Companies, Deals, Activities, API Keys, Signals, Signal Sources, Webhooks)

3. **Authentication Flow (200 OK → 201 Created)**
   - POST /api/v1/auth/register successfully creates users
   - POST /api/v1/auth/login successfully authenticates
   - Returns proper JWT tokens (accessToken + refreshToken)
   - GET /api/v1/auth/me returns user profile
   - Rate limiting headers present (5 req/min for auth, 100 req/min for general)

4. **Static Assets (200 OK)**
   - JS bundle: /assets/index-ZxYbWWxU.js (277KB, properly cached with max-age=31536000)
   - CSS bundle: /assets/index-DuVCBDCG.css (73KB, properly cached)
   - No localhost references detected in bundled JS
   - Assets served with proper cache headers

5. **SPA Routing (200 OK)**
   - /register → returns index.html (client-side routing works)
   - /login → returns index.html (client-side routing works)
   - /pricing → returns index.html (client-side routing works)
   - /terms → returns index.html (client-side routing works)
   - /privacy → returns index.html (client-side routing works)
   - /changelog → returns index.html (client-side routing works)

6. **Security Headers (Excellent)**
   - HSTS enabled: max-age=31536000; includeSubDomains
   - CSP properly configured
   - X-Frame-Options: SAMEORIGIN
   - X-Content-Type-Options: nosniff
   - Cross-Origin-Opener-Policy: same-origin
   - Referrer-Policy: no-referrer

7. **GraphQL Endpoint (200 OK, requires org header)**
   - POST /api/v1/graphql responds correctly
   - Returns proper error for missing X-Organization-Id header
   - Introspection query structure valid

### ⚠️ ISSUES FOUND

1. **CRITICAL: Missing OG Image Asset**
   - Status: /og-image.png returns 200 but serves index.html (3340 bytes)
   - Expected: Should return actual PNG image (1200x630px)
   - Impact: Social media previews on HN/Twitter/LinkedIn will show broken image
   - Fix: Add og-image.png to /Users/paulnispel/headless-crm/frontend/public/

2. **CRITICAL: Missing Favicons**
   - /favicon-32x32.png returns index.html instead of PNG (200 but wrong content)
   - /favicon-16x16.png (not tested, likely same issue)
   - /apple-touch-icon.png (not tested, likely same issue)
   - Impact: Browser tabs will show default/broken favicon
   - Fix: Add favicon PNGs to /Users/paulnispel/headless-crm/frontend/public/

3. **WARNING: Canonical URL Points to Unowned Domain**
   - canonical: https://devsignal.dev
   - og:url: https://devsignal.dev
   - og:image: https://devsignal.dev/og-image.png
   - Current deployment: https://energetic-wisdom-production.up.railway.app
   - Impact: If devsignal.dev is not owned/configured, this will cause 404s for OG image
   - Decision needed: Either register devsignal.dev or update to Railway URL for launch

4. **WARNING: Health Endpoint Missing**
   - GET /api/v1/health returns 404
   - Impact: Cannot programmatically verify API health
   - Recommendation: Add health endpoint for monitoring

5. **INFO: Registration Creates Orphaned Users**
   - Test user created has empty organizations array
   - This is expected behavior but means demo seed cannot be tested without org
   - Not a blocker, but document in onboarding

6. **INFO: Demo Seed Endpoint Not Tested**
   - Could not test POST /api/v1/demo/seed due to missing organization
   - Need valid org context to test this endpoint
   - Recommendation: Test in browser after proper onboarding flow

### 🚨 MUST FIX BEFORE LAUNCH

1. Add og-image.png (1200x630) to frontend/public/
2. Add favicon-32x32.png, favicon-16x16.png, apple-touch-icon.png to frontend/public/
3. Decide on domain strategy:
   - Option A: Register devsignal.dev and point to Railway
   - Option B: Update all URLs to energetic-wisdom-production.up.railway.app
   - Option C: Use custom Railway domain (e.g., app.devsignal.dev)

### 📋 NICE TO HAVE BEFORE LAUNCH

1. Add /api/v1/health endpoint
2. Test demo seed flow in browser
3. Consider adding /api/v1/changelog endpoint to serve changelog.md
4. Add 404 page for non-SPA routes (currently serves index.html for everything)

**Status:** Needs immediate fixes (OG image + favicons) before Show HN launch

**Open questions:**
- Do we own devsignal.dev domain? If not, should we register it or change canonical URLs?
- Should we add a custom Railway domain for cleaner URLs?
